const base_url = 'http://localhost:3001/';
const car_make = base_url + 'api/Make';
const car_model = base_url + 'api/Model';
const filterData = base_url + 'api/Filter';

export default base_url;
export {car_make, car_model, filterData};
